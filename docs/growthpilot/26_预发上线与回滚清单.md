# 26 预发上线与回滚清单

> 用途：Wave 2 QA / 发布负责人在预发前、发布中、回滚时直接照单执行。
>
> 更新时间：`2026-03-25`
>
> 当前 QA 结论基线：API 测试 PASS，迁移 sample artifact 已产出，Web build PASS；SSR runtime smoke 在当前 sandbox 被 `127.0.0.1` 连接权限阻断，需在可开本地端口的环境重跑。

---

## 1. 发布前冻结

- [ ] `07_OpenAPI.yaml`、`17_联调与验收清单.md`、`37_Wave2_QA验收报告.md` 已冻结本次版本
- [ ] 发布分支 / tag / commit 已记录
- [ ] 变更摘要已明确：功能、风险、回滚边界、负责人
- [ ] 已知缺陷与放行规则已同步到验收报告

记录模板：
- 发布波次：`wave-2-qa-gate`
- 发布分支：`<待填>`
- 发布 commit：`<待填>`
- 发布负责人：`<待填>`
- QA：`A10`
- PM / 业务签收：`<待填>`

---

## 2. 预发自检命令

### 2.1 必跑命令

```bash
npm run test --workspace @growthpilot/api
npm run build --workspace @growthpilot/api
npm run build --workspace @growthpilot/web
node scripts/migration/run-staging-import.mjs \
  --dry-run \
  --csv fixtures/staging-import-sample.csv \
  --batchId BATCH-PREPROD-IMPORT-001 \
  --artifacts-dir docs/growthpilot/artifacts/preprod-import
node scripts/qa/run-ssr-smoke.mjs \
  --report-file docs/growthpilot/artifacts/preprod-ssr.json
node scripts/qa/run-billing-permission-smoke.mjs \
  --report-file docs/growthpilot/artifacts/preprod-billing-permission.json
```

### 2.2 当前已验证结果

- [x] `npm run test --workspace @growthpilot/api`
- [x] `npm run build --workspace @growthpilot/api`
- [x] `npm run build --workspace @growthpilot/web`
- [x] sample migration dry-run artifact 已生成
- [ ] `run-ssr-smoke.mjs` runtime PASS
- [ ] `run-billing-permission-smoke.mjs` runtime PASS

说明：
- 本地当前环境执行 `run-ssr-smoke.mjs` 时，Web/API build 已成功，但在启动后访问 `http://127.0.0.1:3101` 遭遇 `EPERM`，属于 sandbox 端口访问限制，不是已确认的应用级 500。

---

## 3. 预发环境检查

### 3.1 服务与配置
- [ ] Web / API 可启动
- [ ] `DATABASE_URL`、`REDIS_URL`、对象存储配置可用
- [ ] `JWT_SECRET` / TTL / S3 bucket 与预发值一致
- [ ] traceId / 错误日志 / job 日志已开启

### 3.2 数据与账号
- [ ] 管理员账号可登录
- [ ] 教师账号可登录
- [ ] 财务 / billing 角色账号可登录
- [ ] sample import 文件已备妥：`fixtures/staging-import-sample.csv`
- [ ] 首批 staging artifact 输出目录已备妥

### 3.3 迁移准备
- [ ] 先跑 sample dry-run
- [ ] 再跑真实批次 `--db-apply`
- [ ] staging 表 row count 与 artifact 对齐
- [ ] reject CSV 已归档并分派 owner

建议命令：

```bash
DATABASE_URL=postgresql://gp:gp_dev@localhost:5432/growthpilot \
node scripts/migration/run-staging-import.mjs \
  --db-apply \
  --csv fixtures/staging-import-sample.csv \
  --batchId BATCH-PREPROD-IMPORT-001 \
  --artifacts-dir docs/growthpilot/artifacts/preprod-import
```

---

## 4. 发布前回归门

### 4.1 P0
- [ ] 登录与 `/auth/me`
- [ ] 学生建档链
- [ ] 作业复核链
- [ ] 成长目标链
- [ ] 账单收款链

### 4.2 P1
- [ ] attendance 设备绑定 / 事件 / 作业时长
- [ ] communication 记录 / 模板 / 消息任务
- [ ] analytics overview / teaching / billing
- [ ] teacher 访问 billing 页面被拒绝

### 4.3 页面级校验
- [ ] `run-ssr-smoke.mjs` 31 页 non-500
- [ ] `run-billing-permission-smoke.mjs` teacher billing 页面返回 forbidden 页面，而不是 500

---

## 5. 备份要求

- [ ] 数据库全量备份完成
- [ ] 对象存储关键前缀快照完成
- [ ] 环境变量 / 部署配置已导出
- [ ] 备份时间、操作者、恢复入口已记录

记录模板：

```text
backup_time: <YYYY-MM-DD HH:mm:ss +08:00>
operator: <name>
database_backup: <path or job id>
object_storage_snapshot: <bucket/prefix or task id>
config_backup: <path>
restore_drill_reference: <doc/link>
```

---

## 6. 发布步骤

1. [ ] 拉取发布 commit / tag
2. [ ] 执行 API / Web build
3. [ ] 执行 sample migration dry-run
4. [ ] 执行真实批次 `--db-apply`
5. [ ] 发布 API
6. [ ] 发布 Web
7. [ ] 执行 API QA 测试与页面 smoke
8. [ ] 记录 artifact、版本号、发布时间、发布人

---

## 7. 回滚触发条件

满足任一条件直接进入回滚判断：
- [ ] 登录主链路不可用超过 5 分钟
- [ ] 学生 / 作业 / billing 任一 P0 断裂
- [ ] teacher 可见 billing 页面或 billing API 越权
- [ ] payment / refund 金额异常
- [ ] `--db-apply` 导致 staging / 正式库异常膨胀或错误覆盖
- [ ] 5xx 持续异常且 10 分钟内无法止血

---

## 8. 回滚方案

### 8.1 应用回滚
1. [ ] 停止新版本 API / Web 流量
2. [ ] 切回上一稳定 commit / 镜像
3. [ ] 清理新版本缓存、队列、临时任务
4. [ ] 重新执行健康检查

### 8.2 数据回滚
1. [ ] 判断是否只涉及 staging 表
2. [ ] 若只涉及 staging：
   - 删除本次 `batch_id` 对应的 staging 数据
   - 保留 artifact 与 reject CSV
3. [ ] 若已触达正式表：
   - 使用发布前数据库备份恢复
   - 校验 payment / refund / report / homework 关键链路
4. [ ] 如附件受影响，恢复对象存储快照

### 8.3 staging-only 回滚 SQL 模板

```sql
begin;
delete from qa_staging.staging_rejects where batch_id = '<BATCH_ID>';
delete from qa_staging.staging_normalized_rows where batch_id = '<BATCH_ID>';
delete from qa_staging.staging_raw_rows where batch_id = '<BATCH_ID>';
delete from qa_staging.import_batches where batch_id = '<BATCH_ID>';
commit;
```

### 8.4 回滚后验证
- [ ] 管理员登录成功
- [ ] 学生列表 / student360 正常
- [ ] homework 详情 / review 状态可读
- [ ] growth report 正常
- [ ] invoice / payment / refund 状态一致
- [ ] 日志恢复正常

---

## 9. 放行结论模板

```markdown
## Go / No-Go Signoff
- 发布范围：
- 发布版本：
- 发布时间：
- QA 结论：pass / conditional-pass / fail
- PM 结论：pass / conditional-pass / fail
- Owner 结论：go / no-go

## 未关闭风险
- P0:
- P1:
- P2:

## 回滚准备
- 备份完成时间：
- 回滚负责人：
- 恢复演练结论：

## 备注
- 已知限制：
- 观察窗口：
```
