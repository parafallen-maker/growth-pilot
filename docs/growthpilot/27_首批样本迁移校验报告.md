# 27 首批样本迁移校验报告

> 对应任务：`QA-09` / `QA-10` / `QA-11` / `T-DM1-2`
>
> 时间：`2026-03-25`
>
> 范围：基于 `fixtures/staging-import-sample.csv` 的首批样本 dry-run，以及基于 mock fallback 的 reject report 验证；同时包含基于 `scripts/migration/run-staging-import.mjs --input scripts/migration/samples/first-wave-sample.json --artifactDir artifacts/migration/BATCH-2026-03-25-json` 产出的首批样本校验结果。

---

## 1. 本轮执行结果

### 1.1 首批样本导入（CSV dry-run）

执行命令：

```bash
node scripts/migration/run-staging-import.mjs \
  --dry-run \
  --csv fixtures/staging-import-sample.csv \
  --batchId BATCH-QA-DOC \
  --artifacts-dir docs/growthpilot/artifacts/2026-03-25
```

结果：
- `batchId`: `BATCH-QA-DOC`
- `sourceSystem`: `file`
- `sourceFile`: `staging-import-sample.csv`
- `mode`: `dry-run`
- `rawRows`: `3`
- `normalizedRows`: `3`
- `readyToLoadRows`: `3`
- `rejectedRows`: `0`

按 domain 就绪分布：
- `students`: `1`
- `homework`: `1`
- `billing`: `1`

幂等性结果：
- `importKeyCount`: `3`
- `uniqueImportKeyCount`: `3`
- `duplicateImportKeys`: `0`
- 策略：`staging upsert by batch/source row identity`

### 1.2 首批样本导入（JSON 样本批次）

执行命令：

```bash
node scripts/migration/run-staging-import.mjs \
  --input scripts/migration/samples/first-wave-sample.json \
  --artifactDir artifacts/migration/BATCH-2026-03-25-json
```

结果：
- `batchId`: `BATCH-2026-03-24`
- `sourceSystem`: `json`
- `sourceFile`: `first-wave-sample.json`
- `mode`: `validation-artifact`
- `artifactDir`: `artifacts/migration/BATCH-2026-03-25-json`

### 1.3 Reject report 验证

执行命令：

```bash
node scripts/migration/run-staging-import.mjs \
  --dry-run \
  --batchId BATCH-QA-MOCK-DOC \
  --artifacts-dir docs/growthpilot/artifacts/2026-03-25/mock-batch
```

结果：
- `rawRows`: `4`
- `readyToLoadRows`: `1`
- `rejectedRows`: `3`
- `reject rows`: `7`

Reject 分类统计：
- `CONFLICT_STUDENT_NO`: `1`
- `FK_TEACHER_MISSING`: `1`
- `FK_STUDENT_MISSING`: `1`
- `DICT_SUBJECT_UNMAPPED`: `1`
- `DICT_ERROR_TAXONOMY_UNMAPPED`: `1`
- `BALANCE_CONTRACT_PAYABLE_MISMATCH`: `1`
- `BALANCE_REFUND_EXCEEDS_PAYMENT`: `1`

---

## 2. 关键结论

### 2.1 字段映射
- sample CSV 已正确完成 `familyStructureRaw -> single_parent`
- sample CSV 已正确完成 `subjectRaw -> math`
- sample CSV 已正确完成 Excel serial 日期转换：
  - `46083 -> 2026-03-02`
  - `46104 -> 2026-03-23`
- billing sample 金额链路通过，未出现 reject

### Plan 摘要（JSON 样本批次）
- rawRows: `6`
- normalizedRows: `6`
- readyToLoadRows: `3`
- rejectedRows: `3`

### 2.2 幂等性
- 同一批次同一源行使用固定 staging upsert key：
  - `batch_id`
  - `source_file`
  - `source_sheet`
  - `source_row_no`
- 脚本已支持 `--db-apply`，设计为重复导入同一批次时更新 staging 行，而不是重复插入
- 本轮在 dry-run / artifact 层已验证 import key 稳定；真实 PostgreSQL 回放仍需在预发库执行一次

### 2.3 Reject report
- reject report 现已由脚本直接生成 CSV，不再停留在模板层
- reject report 字段包含：
  - 源文件 / sheet / row / sourcePk
  - targetDomain / businessKey
  - rejectCode / rejectReason
  - fieldName / rawValue / expectedRule
  - suggestedAction / owner / status

### 当前 ready business keys（JSON 样本批次）
- `S-101`
- `S-101`（学生主档 + 作业事实分别 ready，待 final load 时由映射表收口）
- `I-101`

---

## 3. 产物位置

首批 sample artifact（CSV dry-run）：
- `docs/growthpilot/artifacts/2026-03-25/BATCH-QA-DOC.summary.json`
- `docs/growthpilot/artifacts/2026-03-25/BATCH-QA-DOC.raw.ndjson`
- `docs/growthpilot/artifacts/2026-03-25/BATCH-QA-DOC.normalized.ndjson`
- `docs/growthpilot/artifacts/2026-03-25/BATCH-QA-DOC.reject-report.csv`
- `docs/growthpilot/artifacts/2026-03-25/BATCH-QA-DOC.db-plan.sql`

mock reject artifact：
- `docs/growthpilot/artifacts/2026-03-25/mock-batch/BATCH-QA-MOCK-DOC.summary.json`
- `docs/growthpilot/artifacts/2026-03-25/mock-batch/BATCH-QA-MOCK-DOC.reject-report.csv`

---

## 4. 未完成项

- `QA-09` 的真实 PostgreSQL staging apply 路径已经写入脚本，但本地 sandbox 无可用 `DATABASE_URL`，未执行 live DB apply
- `QA-11` 的"导入后查库校验"仍需在预发 PostgreSQL 上补一次：
  - staging 表 row count
  - upsert 幂等回放
  - reject rows 可查询

---

## 5. 一句话结论

**首批样本已经从"只有 dry-run skeleton"推进到"有样本、有 artifact、有 reject CSV、有真实 DB staging apply 入口"；但真实库回放还没在本地环境完成，因此不能把迁移验收写成 fully green。**

---

## 6. 综合结论

### 已完成
- 首批样本校验报告已形成，且由 repo 内 JSON 样本直接生成正式 artifact
- 异常可追溯到源表、字段、规则、owner
- final load 顺序已验证可执行
- 已同步输出 `summary.json`、`reject-report.csv`、`validation-report.md`、`ready-to-load.json`

### 未完成但不阻塞本任务关闭
- PostgreSQL staging schema
- 正式表 upsert
- Excel xlsx 解析器（当前已支持 JSON/CSV）

> 说明：本任务已从"内置 mock batch dry-run"推进到"真实 JSON/CSV 样本 + 正式 validation artifact"；离真预发还差 staging schema、正式表 upsert 与真实库回放。

**迁移这条线现在不是'没方案'，而是已经有了第一份可追责、可复盘、可交接的校验报告。**
