{
  "name": "@growthpilot/ui",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "format": "node -e \"console.log('ui format placeholder')\"",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node -e \"console.log('ui test placeholder')\""
  }
}
